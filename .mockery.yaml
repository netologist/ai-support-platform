with-expecter: true
mockname: "Mock{{.InterfaceName}}"
outpkg: mocks
resolve-type-alias: false
disable-version-string: true
issue-845-fix: true
packages:
  github.com/netologist/ai-support-platform/internal/app/executor:
    interfaces:
      LoginExecutor:
        config:
          dir: internal/mocks/command
      CreateTicketExecutor:
        config:
          dir: internal/mocks/executor
      UpdateTicketExecutor:
        config:
          dir: internal/mocks/executor
      GetTicketExecutor:
        config:
          dir: internal/mocks/executor
      ListTicketsExecutor:
        config:
          dir: internal/mocks/executor
      IngestDocumentExecutor:
        config:
          dir: internal/mocks/executor
      ListDocumentsExecutor:
        config:
          dir: internal/mocks/executor
      SemanticSearchExecutor:
        config:
          dir: internal/mocks/executor
  github.com/netologist/ai-support-platform/internal/domain/repository:
    interfaces:
      AuthRepository:
        config:
          dir: internal/mocks/repository
      TicketRepository:
        config:
          dir: internal/mocks/repository
      DocumentRepository:
        config:
          dir: internal/mocks/repository
      ChunkRepository:
        config:
          dir: internal/mocks/repository
  github.com/netologist/ai-support-platform/internal/domain/service:
    interfaces:
      AuditLogger:
        config:
          dir: internal/mocks/service
      Authorizer:
        config:
          dir: internal/mocks/service
      MessagePublisher:
        config:
          dir: internal/mocks/service
      PasswordVerifier:
        config:
          dir: internal/mocks/service
      TicketCache:
        config:
          dir: internal/mocks/service
      TokenIssuer:
        config:
          dir: internal/mocks/service
      Chunker:
        config:
          dir: internal/mocks/service
      EmbeddingProvider:
        config:
          dir: internal/mocks/service
      RateLimiter:
        config:
          dir: internal/mocks/service
      AIProvider:
        config:
          dir: internal/mocks/service
